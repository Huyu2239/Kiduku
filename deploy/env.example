# /etc/kiduku/env
# make deploy で参照される環境変数ファイル
# このファイルを /etc/kiduku/env にコピーして値を設定する

# Discord Bot Token（必須）
DISCORD_BOT_TOKEN=

# PostgreSQL 接続文字列（必須）
DATABASE_URL=postgres://kiduku:password@localhost:5432/kiduku

# ログレベル（任意）
RUST_LOG=info

# Discord Gateway シャード数（任意、0 でシャードなし）
SHARD_COUNT=0
